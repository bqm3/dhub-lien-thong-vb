# Legacy pages

Các màn hình demo/template cũ **không còn đăng ký** trên router chính (`src/routes/index.tsx`).

Giữ lại để tham chiếu khi cần. Không import vào menu sidebar.

Cấu trúc đang ship: xem `README.md` ở root repo.
